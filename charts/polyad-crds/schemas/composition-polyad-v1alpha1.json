{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/astrivant/polyad/raw/main/pkg/polyad-schemas/polyad_schemas/resources/composition.v1alpha1.schema.json",
  "title": "polyad.astrivant.com Composition v1alpha1",
  "type": "object",
  "description": "Durable composition intent compiled into owned reusable definitions and one executable root graph. Status carries generated names and UIDs; finalization drains the root before deleting definitions.",
  "properties": {
    "apiVersion": {
      "type": "string",
      "description": "Polyad API group and version.",
      "const": "polyad.astrivant.com/v1alpha1"
    },
    "kind": {
      "type": "string",
      "description": "Custom resource kind: Composition.",
      "const": "Composition"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "type": "object",
      "description": "Immutable request receipt. Use the Flask composition endpoint or Python receipt_spec helper to produce this document.",
      "properties": {
        "requestId": {
          "type": "string",
          "description": "Stable client-generated request identity.",
          "minLength": 1,
          "maxLength": 63,
          "pattern": "^[a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?$"
        },
        "document": {
          "type": "string",
          "description": "Canonical JSON CompositionRequest with rootId and ID-addressed objects. Stored as a string so CEL can enforce immutability even for arbitrary nested workload manifests.",
          "minLength": 2,
          "maxLength": 1048576
        }
      },
      "required": [
        "requestId",
        "document"
      ],
      "x-kubernetes-validations": [
        {
          "rule": "self == oldSelf",
          "message": "Composition intent is immutable; submit a new requestId for different work."
        }
      ]
    },
    "status": {
      "type": "object",
      "default": {},
      "description": "Operator observations and audited identities; refreshed asynchronously.",
      "x-kubernetes-preserve-unknown-fields": true
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "$comment": "Generated from charts/polyad-crds/crds/compositions.yaml; do not edit this artifact."
}
