{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/astrivant/polyad/raw/main/pkg/polyad-schemas/polyad_schemas/resources/shutdownpolicy.v1alpha1.schema.json",
  "title": "polyad.astrivant.com ShutdownPolicy v1alpha1",
  "type": "object",
  "properties": {
    "apiVersion": {
      "type": "string",
      "description": "API group and version of this custom resource: polyad.astrivant.com/v1alpha1.",
      "const": "polyad.astrivant.com/v1alpha1"
    },
    "kind": {
      "type": "string",
      "description": "Custom resource kind: ShutdownPolicy.",
      "const": "ShutdownPolicy"
    },
    "metadata": {
      "type": "object",
      "properties": {}
    },
    "spec": {
      "type": "object",
      "properties": {
        "graceSeconds": {
          "type": "integer",
          "minimum": 0,
          "default": 30,
          "description": "Pod termination grace period in seconds. Applications should drain or checkpoint during SIGTERM or preStop handling before Kubernetes ends this period."
        },
        "afterSeconds": {
          "type": "integer",
          "minimum": 0,
          "description": "Graph lifetime limit in seconds from the Graph CR creation timestamp, including operator downtime. Reaching the limit stops admission and drains owned execution; omission disables the limit."
        }
      },
      "description": "Desired configuration for this ShutdownPolicy resource. Define a reusable graph runtime limit and pod termination grace period. Containers handle termination and durable cleanup; this policy does not execute Python shutdown callbacks."
    },
    "status": {
      "type": "object",
      "x-kubernetes-preserve-unknown-fields": true,
      "description": "Reserved operator observation subresource. This reusable definition does not execute independently or currently publish lifecycle status."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "description": "Define a reusable graph runtime limit and pod termination grace period. Containers handle termination and durable cleanup; this policy does not execute Python shutdown callbacks.",
  "$comment": "Generated from charts/polyad/crds/shutdownpolicies.yaml; do not edit this artifact."
}
