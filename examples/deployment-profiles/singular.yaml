# yaml-language-server: $schema=../../charts/polyad/values.reference.schema.json
# One dense operator in this release's cluster; dependencies remain configurable.
ha: false
operator:
  replicaCount: 1
  autoscaling:
    enabled: false
