{
  "spec": {
    "healthCheckExprs": [
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Activation",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))",
        "current": "(has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && ((status.phase == 'Completed' && has(status.completed) && status.completed) || (status.phase in ['Ready', 'Running'] && has(status.ready) && status.ready))) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Superseded')"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Composition",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))",
        "current": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && ((status.phase == 'Completed' && has(status.completed) && status.completed) || (status.phase in ['Ready', 'Running'] && has(status.ready) && status.ready))"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Daemon",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing && !(has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.serviceLevel) && has(status.serviceLevel.observedGeneration) && status.serviceLevel.observedGeneration == metadata.generation && has(status.serviceLevel.state) && status.serviceLevel.state in ['Degraded', 'Unavailable']))",
        "failed": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.serviceLevel) && has(status.serviceLevel.observedGeneration) && status.serviceLevel.observedGeneration == metadata.generation && has(status.serviceLevel.state) && status.serviceLevel.state in ['Degraded', 'Unavailable']",
        "current": "true"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "DragonflyPool",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "(has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Blocked')",
        "current": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Ready'"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Gate",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "false",
        "current": "true"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Graph",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "!(has(spec.templateOnly) && spec.templateOnly) &&\nhas(status.observedGeneration) && status.observedGeneration == metadata.generation && (\n  (has(status.phase) && status.phase in ['Failed', 'Invalid']) ||\n  (has(status.failed) && status.failed) || (\n    has(status.metrics) && has(status.metrics.observedGeneration) &&\n    status.metrics.observedGeneration == metadata.generation &&\n    has(status.metrics.rollup) && has(status.metrics.rollup.observedGeneration) &&\n    status.metrics.rollup.observedGeneration == metadata.generation && (\n      (has(status.metrics.rollup.failedLeafNodes) && status.metrics.rollup.failedLeafNodes > 0) ||\n      (has(status.metrics.rollup.graphsByPhase) && ['Failed', 'Invalid'].exists(p,\n        p in status.metrics.rollup.graphsByPhase && status.metrics.rollup.graphsByPhase[p] > 0))\n    )\n  )\n)",
        "current": "(has(spec.templateOnly) && spec.templateOnly) || (\n  has(status.observedGeneration) && status.observedGeneration == metadata.generation &&\n  has(status.phase) && (\n    (status.phase == 'Completed' && has(status.completed) && status.completed) ||\n    (status.phase in ['Ready', 'Running'] && has(status.ready) && status.ready)\n  ) &&\n  has(status.metrics) && has(status.metrics.observedGeneration) &&\n  status.metrics.observedGeneration == metadata.generation &&\n  has(status.metrics.rollup) && has(status.metrics.rollup.observedGeneration) &&\n  status.metrics.rollup.observedGeneration == metadata.generation &&\n  has(status.metrics.rollup.observationsComplete) && status.metrics.rollup.observationsComplete &&\n  (!has(status.metrics.rollup.unobservedGraphs) || status.metrics.rollup.unobservedGraphs == 0) &&\n  (!has(status.metrics.rollup.graphsByPhase) ||\n    ['Reconciling', 'Waiting', 'Draining', 'Suspended', 'Stopped', 'Unknown'].all(p,\n      !(p in status.metrics.rollup.graphsByPhase) || status.metrics.rollup.graphsByPhase[p] == 0))\n)"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "GraphPolicy",
        "inProgress": "has(metadata.deletionTimestamp)",
        "failed": "false",
        "current": "true"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "OperatorPool",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "(has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Blocked')",
        "current": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Ready'"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "PolyGraph",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "!(has(spec.templateOnly) && spec.templateOnly) &&\nhas(status.observedGeneration) && status.observedGeneration == metadata.generation && (\n  (has(status.phase) && status.phase in ['Failed', 'Invalid']) ||\n  (has(status.failed) && status.failed) || (\n    has(status.metrics) && has(status.metrics.observedGeneration) &&\n    status.metrics.observedGeneration == metadata.generation &&\n    has(status.metrics.rollup) && has(status.metrics.rollup.observedGeneration) &&\n    status.metrics.rollup.observedGeneration == metadata.generation && (\n      (has(status.metrics.rollup.failedLeafNodes) && status.metrics.rollup.failedLeafNodes > 0) ||\n      (has(status.metrics.rollup.graphsByPhase) && ['Failed', 'Invalid'].exists(p,\n        p in status.metrics.rollup.graphsByPhase && status.metrics.rollup.graphsByPhase[p] > 0))\n    )\n  )\n)",
        "current": "(has(spec.templateOnly) && spec.templateOnly) || (\n  has(status.observedGeneration) && status.observedGeneration == metadata.generation &&\n  has(status.phase) && (\n    (status.phase == 'Completed' && has(status.completed) && status.completed) ||\n    (status.phase in ['Ready', 'Running'] && has(status.ready) && status.ready)\n  ) &&\n  has(status.metrics) && has(status.metrics.observedGeneration) &&\n  status.metrics.observedGeneration == metadata.generation &&\n  has(status.metrics.rollup) && has(status.metrics.rollup.observedGeneration) &&\n  status.metrics.rollup.observedGeneration == metadata.generation &&\n  has(status.metrics.rollup.observationsComplete) && status.metrics.rollup.observationsComplete &&\n  (!has(status.metrics.rollup.unobservedGraphs) || status.metrics.rollup.unobservedGraphs == 0) &&\n  (!has(status.metrics.rollup.graphsByPhase) ||\n    ['Reconciling', 'Waiting', 'Draining', 'Suspended', 'Stopped', 'Unknown'].all(p,\n      !(p in status.metrics.rollup.graphsByPhase) || status.metrics.rollup.graphsByPhase[p] == 0))\n)"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "RemoteScale",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "(has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Blocked')",
        "current": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Ready'"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "ReplicaGroup",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "!(has(spec.templateOnly) && spec.templateOnly) &&\nhas(status.observedGeneration) && status.observedGeneration == metadata.generation && (\n  (has(status.phase) && status.phase in ['Failed', 'Invalid']) ||\n  (has(status.failed) && status.failed) || (\n    has(status.metrics) && has(status.metrics.observedGeneration) &&\n    status.metrics.observedGeneration == metadata.generation &&\n    has(status.metrics.rollup) && has(status.metrics.rollup.observedGeneration) &&\n    status.metrics.rollup.observedGeneration == metadata.generation && (\n      (has(status.metrics.rollup.failedLeafNodes) && status.metrics.rollup.failedLeafNodes > 0) ||\n      (has(status.metrics.rollup.graphsByPhase) && ['Failed', 'Invalid'].exists(p,\n        p in status.metrics.rollup.graphsByPhase && status.metrics.rollup.graphsByPhase[p] > 0))\n    )\n  )\n)",
        "current": "((has(spec.templateOnly) && spec.templateOnly) || (\n  has(status.observedGeneration) && status.observedGeneration == metadata.generation &&\n  has(status.phase) && (\n    (status.phase == 'Completed' && has(status.completed) && status.completed) ||\n    (status.phase in ['Ready', 'Running'] && has(status.ready) && status.ready)\n  ) &&\n  has(status.metrics) && has(status.metrics.observedGeneration) &&\n  status.metrics.observedGeneration == metadata.generation &&\n  has(status.metrics.rollup) && has(status.metrics.rollup.observedGeneration) &&\n  status.metrics.rollup.observedGeneration == metadata.generation &&\n  has(status.metrics.rollup.observationsComplete) && status.metrics.rollup.observationsComplete &&\n  (!has(status.metrics.rollup.unobservedGraphs) || status.metrics.rollup.unobservedGraphs == 0) &&\n  (!has(status.metrics.rollup.graphsByPhase) ||\n    ['Reconciling', 'Waiting', 'Draining', 'Suspended', 'Stopped', 'Unknown'].all(p,\n      !(p in status.metrics.rollup.graphsByPhase) || status.metrics.rollup.graphsByPhase[p] == 0))\n)) && ((has(spec.templateOnly) && spec.templateOnly) || (\n  has(status.scaleCurrent) && status.scaleCurrent &&\n  has(status.observedRemoteScaleIntent) &&\n  status.observedRemoteScaleIntent == (\n    has(metadata.annotations) && 'polyad.astrivant.com/remote-scale-intent' in metadata.annotations\n      ? metadata.annotations['polyad.astrivant.com/remote-scale-intent'] : ''\n  )\n))"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Resource",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "false",
        "current": "true"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Rewrite",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))",
        "current": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.applied) && status.applied"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "ShutdownPolicy",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "false",
        "current": "true"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "TemporaryConnection",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "(has(status.observedGeneration) && status.observedGeneration == metadata.generation && ((has(status.phase) && status.phase in ['Failed', 'Invalid']) || (has(status.failed) && status.failed))) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase == 'Rejected')",
        "current": "has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.phase) && status.phase in ['Active', 'Expired', 'Revoked']"
      },
      {
        "apiVersion": "polyad.astrivant.com/v1alpha1",
        "kind": "Workload",
        "inProgress": "has(metadata.deletionTimestamp) || (has(status.observedGeneration) && status.observedGeneration == metadata.generation && has(status.progressing) && status.progressing)",
        "failed": "false",
        "current": "true"
      }
    ]
  }
}
